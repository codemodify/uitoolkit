# The apps tools/perf measures, one a line:
#   name  package  menu  scroll-x scroll-y
# menu is "click X Y" or "right X Y" (the pointer opens a menu or a popup
# there) or "-" for an app with none. Points are logical pixels of the
# rig's 1280x860 virtual screen, where each app's first window opens in
# the same place every time.
#
# The widget showcase has no command of its own any more — it is a page of
# the tour and the panel under Settings' theme preview — so the row that
# ran `gallery` is gone rather than replaced: `settings` measures the same
# widgets as an embedded pane, and `tour` measures them as a page.
settings  cmd/uitoolkit-settings              click 614 118  435 384
files     examples/uitoolkit-sample-files     click 153 134  650 300
notes     examples/uitoolkit-sample-notes     -              370 500
inspector examples/uitoolkit-sample-inspector click 960 322  600 400
tour      examples/uitoolkit-sample-tour      right 290 104  400 400
richtext  examples/uitoolkit-sample-richtext  click 287 176  640 500
mdi       examples/uitoolkit-sample-mdi       click 187 136  540 390
wizard    examples/uitoolkit-sample-wizard    -              640 450
