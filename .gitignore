*.exe
*.test
*.out
/bin/
dist/
.DS_Store
# Every sample's binary is its directory's name, so one pattern covers
# them all — built beside their source, or at the repo root.
examples/*/uitoolkit-sample-*
cmd/uitksettings/uitksettings
# `go build ./cmd/<name>` from the repo root drops the binary here.
/uitk-themesheet
/uitk-skin
/uitksettings
/uitest-driver
/uitoolkit-sample-*
/settings.png
/tmp/
go.work
go.work.sum

# Claude Cowork session outputs
/Claude outputs/

# Theme Atlas scratch (tools/atlas/render.sh)
tools/atlas/out/
